name = "LuoYunxin1/moongcode"

version = "0.1.0"

readme = "README.md"

repository = "https://github.com/LuoYunxin1/moongcode"

license = "Apache-2.0"

keywords = [ "gcode", "cnc", "toolpath", "safety-preflight" ]

preferred_target = "wasm-gc"

description = "Deterministic G-code modal interpreter, toolpath analyzer, and machine safety preflight for MoonBit"

import {
  "moonbitlang/x@0.4.49",
}
